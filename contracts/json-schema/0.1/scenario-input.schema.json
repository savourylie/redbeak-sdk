{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.redbeak.taiwaneval.com/0.1/scenario-input.schema.json",
  "title": "Scenario input 0.1",
  "x-redbeak-boundary": "runner",
  "description": "One revealed user input for a case execution. Redbeak reveals exactly one input at a time. This object never carries future turns, ground truth, expected outcomes, rubrics, or scoring thresholds; `additionalProperties: false` makes that structurally enforceable rather than a convention.",
  "type": "object",
  "additionalProperties": false,
  "required": ["sequence", "type", "content"],
  "properties": {
    "sequence": { "$ref": "common.schema.json#/$defs/sequence" },
    "type": { "const": "user_message" },
    "content": { "type": "string", "minLength": 1, "maxLength": 100000 },
    "locale": { "type": "string", "pattern": "^[a-z]{2}(-[A-Za-z0-9]{2,8})*$" },
    "deadline_ms": { "$ref": "common.schema.json#/$defs/duration_ms" }
  }
}
